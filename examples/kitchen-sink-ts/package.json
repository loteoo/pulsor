{
  "name": "vite-typescript-starter",
  "version": "0.0.0",
  "scripts": {
    "dev": "pulsor src/app",
    "build": "pulsor build src/app",
    "build:static": "pulsor build src/app --buildTarget=static",
    "build:ssr": "pulsor build src/app --buildTarget=ssr",
    "serve": "pulsor serve",
    "serve:ssr": "pulsor serve --ssr"
  },
  "dependencies": {
    "@pulsor/core": "0.0.1-rc.18",
    "@pulsor/form": "^0.0.1-rc.4",
    "@pulsor/location": "^0.0.1-rc.2"
  }
}
